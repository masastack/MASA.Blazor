@namespace Masa.Blazor.Docs.Examples.components.windows
@implements Masa.Blazor.Docs.Components.IUsageWrapper

<MRow Align="AlignTypes.Center">
    <MItemGroup @bind-Value="window"
                Class="shrink mr-6"
                Mandatory>
        @foreach (var item in Enumerable.Range(1, length))
        {
            <MItem Value="@item">
                <div>
                    <MButton IsActive="@context.Active" Icon OnClick="@context.Toggle">
                        <MIcon>mdi-record</MIcon>
                    </MButton>
                </div>
            </MItem>
        }
    </MItemGroup>

    <MCol>
        @ChildContent
    </MCol>
</MRow>

@code {

    [Parameter]
    [EditorRequired]
    public RenderFragment ChildContent { get; set; } = null!;

    StringNumber window;

    int length = 3;
}
