<MItemGroup>
    <MContainer>
        <MRow>
            @foreach (var i in Enumerable.Range(0, 3))
            {
                <MCol Cols="12" Span="4" @key="i">
                    <MItem>
                        @{
                            <MCard Class="d-flex align-center" 
                                   Color="@(context.Active ? "primary" : "")"
                                   OnClick="context.Toggle" 
                                   Dark 
                                   Height="200">
                                <ScrollYTransition>
                                    <Element Show="context.Active" Class="text-h2 flex-grow-1 text-center">
                                        Active
                                    </Element>
                                </ScrollYTransition>
                            </MCard>
                        }
                    </MItem>
                </MCol>
            }
        </MRow>
    </MContainer>
</MItemGroup>