<MItemGroup Multiple>
    <MContainer>
        <MRow>
            @for (var i = 0; i < 3; i++)
            {
                <MCol Cols="12" Span="4">
                    <MItem>
                        @{
                            <MCard Class="d-flex align-center" Color="@(context.IsActive ? "primary" : "")" Click="context.Toggle" Dark Height="200">
                                @if (context.IsActive)
                                {
                                    <div class="text-h2 flex-grow-1 text-center">
                                        Active
                                    </div>
                                }
                            </MCard>
                        }
                    </MItem>
                </MCol>
            }
        </MRow>
    </MContainer>
</MItemGroup>

