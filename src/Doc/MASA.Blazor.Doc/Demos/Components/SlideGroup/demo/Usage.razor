<MSheet Class="mx-auto" MaxWidth="700">
    <MSlideGroup Multiple ShowArrows="@(true)">
        @foreach (var item in Enumerable.Range(1, 25))
        {
            <MSlideItem>
                <MButton Class="mx-2"
                         IsActive="@context.IsActive"
                         ActiveClass="purple white--text"
                         Depressed
                         Rounded
                         Click="@context.Toggle">
                    Options @item
                </MButton>
            </MSlideItem>
        }
    </MSlideGroup>
</MSheet>