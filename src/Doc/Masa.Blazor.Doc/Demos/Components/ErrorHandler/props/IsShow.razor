<MSwitch @bind-Value="_isShow" Label="@($"IsShow: {_isShow}")"></MSwitch>
<MErrorHandler IsShow="_isShow" >
<MButton @onclick="BtnClick">点击测试</MButton>
</MErrorHandler>
@code
{
    private MErrorHandler mErrorHandler;
    private bool _isShow=true;

    private void BtnClick()
    {
        throw new Exception("throw error!");
    }
}