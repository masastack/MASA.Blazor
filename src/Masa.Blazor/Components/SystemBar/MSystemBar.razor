@namespace Masa.Blazor
@inherits MasaComponentBase

<CascadingValue Value="IsDark" Name="IsDark">
    <IfTransitionElement class="@GetClass()"
                         style="@GetStyle()"
                         ReferenceCaptureAction="e => Ref = e"
                         Value="@If"
                         @onclick="HandleOnClickAsync">
        @ChildContent
    </IfTransitionElement>
</CascadingValue>
