@namespace Masa.Blazor.Presets

<MWindow Value="@_currentAbsolutePath?.Pattern">
    @foreach (var path in _absolutePaths)
    {
        <MWindowItem Transition="" Value="@path.Pattern" @key="path">
            <PWindowContent Value="@path.Self">
                @ChildContent
            </PWindowContent>
        </MWindowItem>
    }
</MWindow>
